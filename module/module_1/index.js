/* let userName = "Mobarok";
console.log(userName);
 */
/* function addNumbers(num1: number, num2: number) {
  console.log(num1 + num2);
}
addNumbers(12, 12); */
//any ->super type
//built in type -> number,string,boolean,void,null,undefined etc.
//user-defined types -> Arrays,Enums,Classes,interfaces etc.
//builtin type
//if we not use data type then typescript dhore ney eta any type ->super type
/* let userId: number;
let userName: string;
let isActivated: boolean;

userId = 1001;
userName = "Mobarok Ali";
isActivated = false;

const userInfo = {
  userId,
  userName,
  isActivated,
};



function printFun() {
  console.log(userInfo);
}

/* /* printFun(); */
function display() {
    console.log("I am displsy");
}
display();
